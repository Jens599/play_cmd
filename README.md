# play-cmd

Python port of `Start-MPVStream`: a terminal-first `mpv`/`yt-dlp` launcher with a modern TUI.

## Development

```powershell
uv sync
uv run play --help
uv run pytest
uv run ruff check .
```
